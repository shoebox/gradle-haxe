apply plugin: 'groovy'

dependencies
{
    compile gradleApi()
    compile localGroovy()
}

group = 'org.shoebox'
version = '0.1.0'

tasks.create(name: 'compileJar', type: Copy, dependsOn:'jar') {
    from(file('build/libs/'))
    into(".")
    include('**/*.jar')
}
